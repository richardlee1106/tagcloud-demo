# 特大错误解决开发计划

> 目标仓库：`D:\AAA_Edu\TagCloud\vite-project`
>  
> 核心问题：PRD 是多智能体协作架构，但当前执行层仍以 `task-orchestrator.js` 单体瀑布流为主，尚未完成 v2 架构形态闭环。

---

## 1. 计划目标（不改目标，只改实现）

在不破坏对外契约的前提下，把 V2 从“中心协调器调用一串函数”升级为“事件驱动状态推进器 + lane handlers + 可插拔 specialist 协作单元”。

本计划完成后，应满足：

1. `task-orchestrator.js` 不再承载三套大流程函数（structured / buffer / legacy）的重复编排细节。
2. `objective contract` 从“描述信息”升级为“执行门禁”（`must_cover`、`latency_budget_ms` 必须影响执行路径）。
3. `specialist` 从静态分发表升级为可注册、可治理、可扩展的插件式单元。

---

## 2. v2 架构不偏航红线

以下红线任何一条被破坏，都视为偏离 v2 方向：

1. 对外接口保持兼容：`POST /api/v2/analysis`、`GET /api/v2/jobs/:jobId`、SSE 事件名不变（`fast.result`、`deep.accepted`、`deep.patch`、`deep.final`、`deep.failed`）。
2. `Task Orchestrator` 仍是唯一任务入口和对外输出出口。
3. 片区分析类 objective 仍必须先经 grounding，再允许质量裁决。
4. `Quality Guard=handoff_legacy` 时，必须停止新路径 deep 收益承诺。
5. 非 artifact 任务不得输出 artifact 可用措辞。

---

## 3. 范围与非目标

### 范围（本次必须完成）

1. 编排内核事件化重构（P2 主线）。
2. objective contract 强门禁化。
3. specialist 插件化与执行治理（超时/降级/并行策略）。
4. 重复流程折叠与回归测试补齐。

### 非目标（本次不做）

1. 新增 objective 类型。
2. 大规模改写前端协议。
3. 全量替换路由策略（规则/LLM 比例调整可留后续）。
4. 彻底移除 legacy 路径（仅收敛其边界，不删除）。

---

## 4. 分阶段开发计划

## Phase 0：基线冻结与护栏建立（0.5 天）

### 目标

锁定“可证明不偏航”的验收基线，避免边重构边漂移。

### 文件

- 修改：`V2-Agent-backend/tests/task-orchestrator-event-bus.test.js`
- 修改：`V2-Agent-backend/tests/task-orchestrator-quality-guard.test.js`
- 修改：`V2-Agent-backend/tests/task-orchestrator-degraded.test.js`
- 新增：`V2-Agent-backend/tests/task-orchestrator-contract-compat.test.js`

### 验收

1. 事件顺序、job 快照字段、handoff 行为都有回归断言。
2. 对外契约断言先行失败（红），后续阶段逐步转绿。

---

## Phase 1：编排内核抽离（2 天）

### 目标

把 `task-orchestrator.js` 从“流程实现体”降为“组装入口 + 路由分发器”。

### 文件

- 新增：`V2-Agent-backend/src/orchestrator/analysis-events.js`
- 新增：`V2-Agent-backend/src/orchestrator/orchestration-kernel.js`
- 新增：`V2-Agent-backend/src/orchestrator/lane-handlers/structured-lane-handler.js`
- 新增：`V2-Agent-backend/src/orchestrator/lane-handlers/buffer-export-lane-handler.js`
- 新增：`V2-Agent-backend/src/orchestrator/lane-handlers/legacy-lane-handler.js`
- 重构：`V2-Agent-backend/src/orchestrator/task-orchestrator.js`

### 实施要点

1. 用“事件 -> 状态推进 -> 副作用执行”替代三套大过程函数中的重复逻辑。
2. 状态推进统一走内核，不在各 lane 内手写大量 `machine.transition(...)`。
3. lane handler 只保留 objective 差异逻辑，不重复写持久化、事件发射、deep 队列模板。

### 验收

1. `task-orchestrator.js` 行数与职责明显收敛（仅保留组装/入口/分发）。
2. 原有 `task-orchestrator-*` 回归测试通过。

---

## Phase 2：Objective Contract 强门禁化（1.5 天）

### 目标

让 `must_cover`、`latency_budget_ms`、`artifact_policy` 真正控制执行行为。

### 文件

- 修改：`V2-Agent-backend/src/contracts/objective-contract.js`
- 修改：`V2-Agent-backend/src/runtime/execution-policy.js`
- 新增：`V2-Agent-backend/src/runtime/objective-gate.js`
- 修改：`V2-Agent-backend/src/orchestrator/lane-handlers/structured-lane-handler.js`
- 修改：`V2-Agent-backend/src/orchestrator/lane-handlers/buffer-export-lane-handler.js`
- 新增测试：`V2-Agent-backend/tests/objective-gate.test.js`

### 实施要点

1. `must_cover` 驱动 specialist 任务裁剪，缺失 section 触发质量降级或 fallback。
2. `latency_budget_ms` 映射为 fast/deep 截止与可降级策略，不再只入 telemetry。
3. `artifact_policy` 统一拦截 artifact 文案与字段输出。

### 验收

1. 门禁失败路径可被测试稳定复现。
2. 通过门禁后的结果与当前对外契约兼容。

---

## Phase 3：Specialist 插件化（2 天）

### 目标

把 `specialist-catalog.js` 从静态 handler map 升级为注册中心。

### 文件

- 重构：`V2-Agent-backend/src/orchestrator/specialist-catalog.js`
- 新增：`V2-Agent-backend/src/orchestrator/specialist-registry.js`
- 修改：`V2-Agent-backend/src/orchestrator/parallel-agent-executor.js`
- 新增：`V2-Agent-backend/tests/specialist-registry.test.js`
- 修改：`V2-Agent-backend/tests/parallel-agent-executor.test.js`

### 实施要点

1. 定义统一注册接口：`id / supports_objectives / run / timeout / fallback_policy`。
2. 执行器按 registry 任务运行，失败按策略降级，不再硬编码分发表。
3. 保留兼容适配层，避免一次性改断现有 specialist。

### 验收

1. 新增一个 mock specialist 可“零改 orchestrator 主体”接入并被调度。
2. `unknown_specialist`、超时、worker 降级路径均有覆盖。

---

## Phase 4：重复流程折叠与收口（1.5 天）

### 目标

彻底消除 structured / buffer / legacy 三套重复流程中的公共拼装代码。

### 文件

- 修改：`V2-Agent-backend/src/orchestrator/task-orchestrator.js`
- 新增：`V2-Agent-backend/src/orchestrator/lane-shared.js`
- 修改：`V2-Agent-backend/src/runtime/lane-state-machine.js`
- 新增：`V2-Agent-backend/tests/lane-shared.test.js`

### 实施要点

1. 抽出统一的 `emitFast` / `enqueueDeep` / `persistSnapshot` / `degradeRun`。
2. 每个 lane 只实现“业务差异输入”，不再重复实现“编排骨架”。
3. 状态流转与事件语义对齐，保障 `/jobs/:jobId` 与 SSE 终态一致。

### 验收

1. 三条 lane 公共逻辑集中到共享层。
2. 关键行为回归测试不下降。

---

## Phase 5：集成验证与发布门（1 天）

### 目标

确保“架构升级”没有引入契约回退、性能退化或可观测盲区。

### 验收命令

1. `cd V2-Agent-backend && node --test`
2. `cd D:\AAA_Edu\TagCloud\vite-project && npm test`
3. `cd D:\AAA_Edu\TagCloud\vite-project && npm run build`

### 发布门标准

1. 对外契约回归 100% 通过。
2. 新增门禁/插件化测试全部通过。
3. fast lane 首包与 deep lane 失败隔离行为不倒退。

---

## 5. 里程碑定义（供评审）

1. **M1（结构里程碑）**：`task-orchestrator.js` 完成“入口化”，lane handler 成型。
2. **M2（契约里程碑）**：objective contract 成为执行门禁而非注释字段。
3. **M3（协作里程碑）**：specialist 可插拔，新增 agent 不再改中心分发表。
4. **M4（发布里程碑）**：契约兼容、测试通过、可观测闭环完整。

---

## 6. 风险与应对

1. 风险：重构期间 SSE 事件顺序漂移。  
   应对：Phase 0 先固化事件顺序测试。
2. 风险：插件化导致执行时序不稳定。  
   应对：执行器增加确定性排序与超时降级测试。
3. 风险：门禁过严导致新路径命中率下降。  
   应对：引入审计日志，先观测后调阈值，不直接放宽红线。
4. 风险：legacy 回退被误删或失效。  
   应对：保留 handoff 回归集，作为发布阻断项。

---

## 7. 最终验收清单（你的评审重点）

1. 是否实现了“事件驱动状态推进器 + lane handlers + agent subscribers”。
2. 是否把 objective contract 从声明变成执行门禁。
3. 是否把 specialist 从静态分发表变成可插拔协作单元。
4. 是否在全程保持对外契约兼容与 fallback 安全网。

