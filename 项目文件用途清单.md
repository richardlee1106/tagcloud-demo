# 项目文件用途清单（详细版）

> 统计范围：当前仓库可维护文件，已排除 `node_modules`、`dist`、`.git`、`__pycache__` 等依赖或缓存目录。
> 文件总数：148

说明：代码文件优先按“业务职责”描述，配置/数据/日志文件按“实际使用场景”描述。

## .

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `无关内容回答模板.md` | Markdown | 非地理相关输入的回复模板文案，供 relevance gate 统一复用。 |
| `项目文件用途清单.md` | Markdown | 项目全量文件用途清单（本文件），用于快速定位模块职责。 |
| `预分类规则.md` | Markdown | 输入预分类规则说明，定义问题类型与处理分流策略。 |
| `﻿AGENT.md` | Markdown | 说明文档或记录文件，用于沉淀规则、流程和项目知识。 |
| `CLAUDE.md` | Markdown | 项目协作规范文档，集中记录团队约定、开发流程和交付标准。 |
| `docker-compose.prod.yml` | YAML | 生产部署编排文件，包含更接近线上拓扑的服务组合与运行参数。 |
| `docker-compose.spatial.yml` | YAML | 空间分析专项编排文件，偏向 Spatial-RAG 调试场景。 |
| `docker-compose.yml` | YAML | 本地开发编排文件，一键拉起前后端、数据库及相关依赖服务。 |
| `fastify-backend.tar.gz` | 压缩归档 | 后端目录历史归档包，用于备份或离线迁移。 |
| `fix_stage.py` | Python | 一次性修复脚本，用于批量替换 AiChat 阶段步骤文案与图标定义。 |
| `index.html` | 文件 | 前端宿主页，Vite 在此注入构建产物并挂载 Vue 应用。 |
| `nul` | 文件 | 项目文件。 |
| `package-lock.json` | JSON | 前端依赖锁文件，保证安装版本可复现。 |
| `package.json` | JSON | 前端工程清单，定义 Vite 开发/构建脚本与依赖。 |
| `pgvector_readme.md` | Markdown | pgvector 使用说明与安装参考文档。 |
| `README.md` | Markdown | 项目总览文档，说明架构、启动方式、接口与运维脚本。 |
| `vercel.json` | JSON | Vercel 部署配置，定义路由重写、函数行为和发布规则。 |
| `vite.config.js` | JavaScript | Vite 构建配置，包含代理、分包策略与前端打包优化规则。 |

## api/ai

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `chat.js` | JavaScript | Vercel Serverless API 入口，用于云端部署时提供轻量接口兼容。 |
| `search.js` | JavaScript | Vercel Serverless API 入口，用于云端部署时提供轻量接口兼容。 |
| `status.js` | JavaScript | Vercel Serverless API 入口，用于云端部署时提供轻量接口兼容。 |

## api/category

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `tree.js` | JavaScript | Vercel Serverless API 入口，用于云端部署时提供轻量接口兼容。 |

## api/spatial

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `fetch.js` | JavaScript | Vercel Serverless API 入口，用于云端部署时提供轻量接口兼容。 |

## deploy

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| ` .env` | 环境配置 | 部署环境变量模板，提供容器化启动所需的数据库、服务端口与密钥配置占位。 |

## deploy/docker-init

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `01-init-schema.sql` | SQL | 数据库初始化脚本，在容器首次启动时创建核心表结构、索引或扩展。 |

## docker

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `Dockerfile.db` | 文件 | 数据库镜像构建文件，用于定制 PostGIS/pgvector 等空间能力依赖。 |

## docs

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `python-migration-status.md` | Markdown | Node 计算迁移到 Python 计算引擎的阶段状态、性能对比与收敛结论。 |

## fastify-backend

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `Dockerfile` | 文件 | Fastify 网关镜像构建文件，定义后端运行时依赖与启动方式。 |
| `ensure_indices.sql` | SQL | 索引补齐脚本，用于对现有库进行性能修复与缺失索引补建。 |
| `package-lock.json` | JSON | 后端依赖锁定文件，确保部署与本地安装的版本一致性。 |
| `package.json` | JSON | 后端工程清单，定义 Fastify 服务脚本、依赖与运行入口。 |
| `server.js` | JavaScript | Fastify 网关主入口，初始化数据库、队列、路由并管理优雅停机。 |

## fastify-backend/docs

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `python_service_architecture.md` | Markdown | Python 计算侧车服务的架构说明，覆盖分层结构、协议和模块职责。 |

## fastify-backend/fastify-backend/reports/rollout

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `dual-run-latest.json` | JSON | 双跑校验的嵌套输出快照（历史遗留路径），用于排查脚本输出目录异常。 |

## fastify-backend/proto

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `spatial_compute.proto` | Protocol Buffers | Node 与 Python 之间 gRPC 协议契约，定义请求体、事件流与服务接口。 |

## fastify-backend/python_service

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `__init__.py` | Python | python_service 包标记文件，支持模块化导入。 |
| `app.py` | Python | Python 侧 HTTP 入口，提供健康检查与基础运行指标接口。 |
| `grpc_server.py` | Python | Python gRPC 服务入口，接收 Node 请求并流式返回阶段事件。 |
| `requirements.txt` | 文件 | Python 计算服务依赖清单（gRPC、PostGIS、聚类、几何与 H3 相关库）。 |

## fastify-backend/python_service/algorithms

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `__init__.py` | Python | 算法子包标记文件，组织空间算法模块导出。 |
| `alpha_shape.py` | Python | 区域边界生成算法，基于 alpha-shape 并带凸包兜底与点集降采样。 |
| `direction_filter.py` | Python | 方向语义过滤模块，将“东南西北/附近方位”映射为 POI 筛选约束。 |
| `graph_reasoning.py` | Python | 空间图推理模块，构图后输出连通性、枢纽节点与网络结构指标。 |
| `h3_aggregate.py` | Python | H3 网格聚合模块，统计网格热点并在缺失 h3 库时提供规则化 fallback。 |
| `hdbscan_cluster.py` | Python | 聚类模块，优先 HDBSCAN 并支持 DBSCAN 兜底与参数自适应。 |
| `membership.py` | Python | 模糊区域隶属度评分模型，综合密度、纯度、中心性等指标输出层级。 |
| `region_comparison.py` | Python | 多选区对比模块，按区域统计类别结构并生成可比较指标。 |

## fastify-backend/python_service/db

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `__init__.py` | Python | 数据库访问子包标记文件。 |
| `repository.py` | Python | Python 侧 PostGIS 仓储层，封装按范围/分类/区域读取 POI 的 SQL。 |

## fastify-backend/python_service/generated

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `spatial_compute_pb2_grpc.py` | Python | 由 proto 生成的 gRPC 服务桩代码，供服务端/客户端调用。 |
| `spatial_compute_pb2.py` | Python | 由 proto 生成的消息定义文件，供 Python gRPC 序列化与反序列化。 |

## fastify-backend/python_service/pipeline

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `__init__.py` | Python | 计算管线子包标记文件。 |
| `spatial_pipeline.py` | Python | Python 计算主流水线，编排候选读取、聚类、边界、图推理与结果组装。 |

## fastify-backend/python_service/tests

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `test_spatial_pipeline.py` | Python | Python 管线基础测试，验证请求在最小输入下能产出 FINAL 事件。 |

## fastify-backend/reports/rollout

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `dual-run-latest.json` | JSON | 迁移灰度或回退演练的机器可读结果快照，用于脚本比对、告警阈值判断与回归追踪。 |
| `dual-run-latest.md` | Markdown | 迁移灰度或回退演练的人工分析报告，记录现象、风险、结论与后续动作。 |
| `fallback-drill-latest.json` | JSON | 迁移灰度或回退演练的机器可读结果快照，用于脚本比对、告警阈值判断与回归追踪。 |
| `fallback-drill-latest.md` | Markdown | 迁移灰度或回退演练的人工分析报告，记录现象、风险、结论与后续动作。 |
| `migration-closeout-20260208.md` | Markdown | 迁移灰度或回退演练的人工分析报告，记录现象、风险、结论与后续动作。 |
| `ops-hardening.md` | Markdown | 迁移灰度或回退演练的人工分析报告，记录现象、风险、结论与后续动作。 |
| `phase-10.json` | JSON | 迁移灰度或回退演练的机器可读结果快照，用于脚本比对、告警阈值判断与回归追踪。 |
| `phase-10.md` | Markdown | 迁移灰度或回退演练的人工分析报告，记录现象、风险、结论与后续动作。 |
| `phase-100.json` | JSON | 迁移灰度或回退演练的机器可读结果快照，用于脚本比对、告警阈值判断与回归追踪。 |
| `phase-100.md` | Markdown | 迁移灰度或回退演练的人工分析报告，记录现象、风险、结论与后续动作。 |
| `phase-30.json` | JSON | 迁移灰度或回退演练的机器可读结果快照，用于脚本比对、告警阈值判断与回归追踪。 |
| `phase-30.md` | Markdown | 迁移灰度或回退演练的人工分析报告，记录现象、风险、结论与后续动作。 |
| `phase-60.json` | JSON | 迁移灰度或回退演练的机器可读结果快照，用于脚本比对、告警阈值判断与回归追踪。 |
| `phase-60.md` | Markdown | 迁移灰度或回退演练的人工分析报告，记录现象、风险、结论与后续动作。 |
| `source-policy-bias-fix.md` | Markdown | 迁移灰度或回退演练的人工分析报告，记录现象、风险、结论与后续动作。 |

## fastify-backend/routes

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `category.js` | JavaScript | 分类路由：提供树形分类和扁平分类接口给前端筛选器。 |
| `search.js` | JavaScript | 快速检索路由：绕过完整 LLM 流程，直接走数据库做轻量关键词检索。 |

## fastify-backend/routes/ai

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `executor.js` | JavaScript | AI 三阶段中的执行器：接收 QueryPlan 后调用 Python gRPC 执行空间计算并标准化结果。 |
| `index.js` | JavaScript | AI 兼容路由入口：承接前端聊天接口、SSE 推送与 Jobs 体系衔接。 |
| `planner.js` | JavaScript | AI 规划器：解析用户问题生成结构化 QueryPlan，并接入输入相关性闸门。 |
| `writer.js` | JavaScript | AI 写作器：基于计算结果生成自然语言解释，控制语气、长度和输出结构。 |

## fastify-backend/routes/jobs

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `index.js` | JavaScript | 异步任务协议路由：负责创建任务、查询状态、SSE 订阅与结果拉取。 |

## fastify-backend/routes/spatial

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `index.js` | JavaScript | 空间 API 路由：组织地理约束、语义检索、gRPC 计算和结果回传。 |

## fastify-backend/scripts

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `bench_jobs.js` | JavaScript | Jobs 压测脚本，输出平均值与 p50/p95 等时延指标。 |
| `dev_stack.js` | JavaScript | 本地开发编排脚本，一次启动 Python gRPC、后端与可选 Worker。 |
| `import_extra_layers.js` | JavaScript | 附加图层导入脚本，将道路/用地等 GeoJSON 写入 PostGIS 表。 |
| `import_poi_data.js` | JavaScript | POI 导入脚本，解析原始数据并写入数据库与向量索引。 |
| `migrate_to_pgvector.js` | JavaScript | 向量迁移脚本，将历史向量流程统一迁移到 PostgreSQL pgvector。 |
| `smoke_jobs_flow.js` | JavaScript | Jobs 冒烟脚本，验证任务创建、完成与结果结构是否可用。 |
| `verify_vectors.js` | JavaScript | 向量库自检脚本，核验 pgvector 扩展、表结构和向量数据完整性。 |

## fastify-backend/services

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `catalog.js` | JavaScript | 分类服务：从数据库动态构建分类树，空库时回退静态 catalog。 |
| `categoryOntology.js` | JavaScript | 分类本体与同义词模块，支撑语义扩展、类别归并和意图识别。 |
| `database.js` | JavaScript | 数据库服务核心层，管理连接池并提供 PostGIS 读写与查询能力。 |
| `geocoder.js` | JavaScript | 地理编码服务，通过 POI、缓存与外部 API 多策略解析地名锚点。 |
| `grpcClient.js` | JavaScript | Node 侧 gRPC 客户端封装，统一处理流式事件与最终结果。 |
| `llm.js` | JavaScript | LLM 网关服务，封装本地/云端模型选择、可用性探测与请求发送。 |
| `migrationPolicy.js` | JavaScript | 迁移策略模块，按配置输出 Python 主路径和灰度决策快照。 |
| `queryCache.js` | JavaScript | 查询缓存模块，按空间指纹缓存计算结果并维护 TTL 与命中统计。 |
| `queue.js` | JavaScript | 任务队列服务，支持 BullMQ + Redis 与内存降级两种运行模式。 |
| `ragLogger.js` | JavaScript | RAG 过程日志服务，记录一次查询从检索到生成的全过程诊断信息。 |
| `relevanceGate.js` | JavaScript | 输入相关性闸门，识别非地理问题并返回统一友好提示文本。 |
| `sourcePolicy.js` | JavaScript | 来源策略模块，统一 UI 选区/分类约束并生成可解释 source_policy。 |
| `spatialJobRunner.js` | JavaScript | 空间任务编排核心，串联 planner、compute、writer、缓存和降级策略。 |
| `vectordb.js` | JavaScript | 向量数据库服务，封装 pgvector 初始化、写入与相似检索能力。 |

## fastify-backend/sql

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `01_spatial_intelligence.sql` | SQL | 空间智能基础 SQL，创建空间分析所需表、函数或初始结构。 |
| `02_postgis_performance.sql` | SQL | PostGIS 性能优化 SQL，包含索引、统计信息或查询路径优化。 |
| `03_partition_pois_v2.sql` | SQL | POI 分区方案 SQL，按策略拆分数据表以提升大数据量查询性能。 |
| `04_spatial_composite_index.sql` | SQL | 复合空间索引 SQL，优化空间条件与类别条件混合查询。 |

## fastify-backend/tests

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `relevanceGate.test.js` | JavaScript | relevanceGate 单元测试，覆盖噪声输入识别和友好提示触发逻辑。 |

## fastify-backend/workers

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `spatialWorker.js` | JavaScript | 空间任务 Worker 入口，消费队列任务并调用 spatialJobRunner 执行。 |

## Log

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `第八版更新日志-AI架构重构.md` | Markdown | 阶段开发日志，记录该版本迭代的目标、实现细节、问题与复盘。 |
| `第二版开发日志.md` | Markdown | 阶段开发日志，记录该版本迭代的目标、实现细节、问题与复盘。 |
| `第六版开发日志.md` | Markdown | 阶段开发日志，记录该版本迭代的目标、实现细节、问题与复盘。 |
| `第七版开发日志—AI助手集成.md` | Markdown | 阶段开发日志，记录该版本迭代的目标、实现细节、问题与复盘。 |
| `第三版开发日志.md` | Markdown | 阶段开发日志，记录该版本迭代的目标、实现细节、问题与复盘。 |
| `第四版开发日志.md` | Markdown | 阶段开发日志，记录该版本迭代的目标、实现细节、问题与复盘。 |
| `第五版开发日志.md` | Markdown | 阶段开发日志，记录该版本迭代的目标、实现细节、问题与复盘。 |
| `第一版开发日志.md` | Markdown | 阶段开发日志，记录该版本迭代的目标、实现细节、问题与复盘。 |
| `移动版适配日志.md` | Markdown | 阶段开发日志，记录该版本迭代的目标、实现细节、问题与复盘。 |
| `AI集成开发日志.md` | Markdown | 阶段开发日志，记录该版本迭代的目标、实现细节、问题与复盘。 |
| `APPLICATION_LOGIC.md` | Markdown | 阶段开发日志，记录该版本迭代的目标、实现细节、问题与复盘。 |
| `GeoLoom V1版本构建日志.md` | Markdown | 阶段开发日志，记录该版本迭代的目标、实现细节、问题与复盘。 |

## newdata

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `武汉路网.geojson` | GeoJSON 数据 | 外部补充空间图层数据，用于增强底图分析或专题可视化。 |
| `EULUC用地情况.geojson` | GeoJSON 数据 | 外部补充空间图层数据，用于增强底图分析或专题可视化。 |

## public

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `logo.svg` | SVG 资源 | 应用 Logo 资源。 |
| `vite.svg` | SVG 资源 | Vite 默认图标资源（项目模板保留）。 |

## public/data

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `武汉POP.tif.aux.xml` | XML | 人口栅格辅助元数据文件，配合 GeoTIFF 读取坐标系和范围信息。 |

## scripts/sql

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `create_database.sql` | SQL | 数据库创建脚本，初始化 tagcloud/geoloom 数据库实例。 |
| `init_database.sql` | SQL | 数据库初始化脚本，创建 POI 主表、索引、扩展与辅助表。 |

## src

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `App.vue` | Vue 组件 | Vue 根组件，仅承载路由出口并维持全局页面容器。 |
| `config.js` | JavaScript | 前端运行配置，按开发/生产环境切换 API 基地址。 |
| `main.js` | JavaScript | Vue 应用启动入口，注册全局组件、路由与 UI 插件后挂载应用。 |
| `MainLayout.vue` | Vue 组件 | 主工作台页面，组织地图、标签云、AI 面板与布局联动逻辑。 |
| `style.css` | 文件 | 全局样式文件，定义页面基础样式和公共视觉规则。 |

## src/assets

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `vue.svg` | SVG 资源 | Vue 图标静态资源。 |

## src/components

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `AiChat.vue` | Vue 组件 | AI 对话主组件，处理消息气泡、SSE 流式渲染、阶段进度和交互动作。 |
| `ControlPanel.vue` | Vue 组件 | 控制面板组件，管理分类筛选、查询触发、抽屉与移动端控件。 |
| `EmbeddedTagCloud.vue` | Vue 组件 | 嵌入式标签云组件，提供轻量标签布局与地图联动入口。 |
| `MapContainer.vue` | Vue 组件 | 地图容器组件，封装 OpenLayers 地图、图层渲染、绘制与交互控制。 |
| `SpatialEvidenceCard.vue` | Vue 组件 | 空间证据卡片组件，展示热点区/区域对比/模糊区域并可一键追问。 |
| `TagCloud.vue` | Vue 组件 | 主标签云组件，整合多种 worker 布局算法、缩放交互和权重渲染。 |

## src/composables

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `useRegions.js` | JavaScript | 多选区状态管理 composable，负责区域增删改、上限控制和共享状态。 |

## src/router

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `index.js` | JavaScript | 前端路由定义，切换主工作台与叙事模式页面。 |

## src/shims

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `child-process-browser.js` | JavaScript | 浏览器端 child_process 垫片，避免依赖在前端打包时报错。 |

## src/utils

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `aiService.js` | JavaScript | 前端 AI 服务层，封装对后端聊天接口的请求与 SSE 事件解析。 |
| `DeckGLOverlay.js` | JavaScript | deck.gl 与 OpenLayers 叠加桥接器，用于高性能 WebGL 图层叠加渲染。 |
| `RasterExtractor.js` | JavaScript | 栅格值提取工具，从 GeoTIFF 中按经纬度读取人口等栅格值。 |
| `toonFormat.js` | JavaScript | TOON 轻量格式工具，负责 GeoJSON 与紧凑文本格式的双向转换。 |

## src/views

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `NarrativeMode.vue` | Vue 组件 | 叙事模式页面，结合地图与动效进行 AI 空间讲解与分步播放。 |

## src/workers

| 文件 | 类型 | 用途（详细） |
|---|---|---|
| `basic.worker.js` | JavaScript | 基础标签布局 worker，执行动态重心布局并避免阻塞主线程。 |
| `dataLoader.worker.js` | JavaScript | 数据加载 worker，异步请求后端空间接口并回传 POI 要素。 |
| `geo.worker.js` | JavaScript | 地理感知布局 worker，按方位与距离约束生成空间一致的标签布局。 |
| `gravity.worker.js` | JavaScript | 重力/圈层布局 worker，处理大规模标签的自适应分布。 |
| `spiral.worker.js` | JavaScript | 螺旋布局 worker，适用于高密度标签的紧凑排布。 |

