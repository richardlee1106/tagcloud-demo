好，我已经看完 GLM 的反馈，我的判断出来了，而且结论和 GLM 的诊断完全相反。
P1C-Fix 分析决策
核心诊断：DirAcc 22% 是「正常的」，不是 bug
先调出 CHANGELOG 里 P0 的完整训练曲线，对比一下：
Epoch	P0 的 DirAcc（100% 数据）	P1C-Fix DirAcc（10% 数据）
10	18.8%	——
15	26.0%	——
20	42.4%	——
25	49.4%	——
30	54.1%	22.03%
35	66.6%	——
50	64.4%	——
80	68.41%	——
P0 用了 100% 数据 + 80 epochs 才让 DirAcc 稳定在 68%。
更关键的：P0 Phase 1 也是 10% 数据 50 epochs，结果 DirAcc = 22.53%。
现在 P1C-Fix 是 10% 数据 30 epochs，DirAcc = 22.03%——完全符合规律，说明方向训练配置没有任何问题。
GLM 诊断的「方向监督信号没有正确传播」是一个误判，被数字吓到了。
其他指标解读
指标	P0 基线	P1C-Fix	判断
Region Sep	0.65	1.0753	✅ 上升明显，说明对比学习确实在起作用，功能区开始分离
Region F1	21.64%	23.68%	✅ 小幅提升（10% 数据 30 epochs 能有这个结果很好）
Pearson	0.9931	0.9840	✅ 下降 <1%，可接受
Overlap@K	48.75%	30.19%	⚠️ 下降，但这是 10% 小样本效应，全量时会恢复
决策：直接进入全量训练，不需要再调参
10% 数据的小规模实验已经验证了：

集成脚本逻辑正确（Region Sep 上升证明对比学习在起作用）
方向训练参数正确（DirAcc = 22% 符合 10%+30 epochs 的历史规律）
Pearson 下降在可接受范围
不需要： 进一步调 region_weight、调学习率、继续调小规模。

————————————————————————————————————————————————————————————————
给 GLM 的下一步指令：
任务名称：P1C-Full 全量集成训练
背景说明：
  P1C-Fix 小规模验证通过。DirAcc = 22% 是正常现象，
  P0 在 10% 数据 50 epochs 时同样只有 22.53%，属于正常收敛规律。
  现在可以直接进行全量训练。
训练命令：
  python experiment_p1c_integrated.py --sample 1.0 --epochs 80 --batch 16384
训练中每 10 个 epoch 记录一次指标（与 P0 对比）
汇报给 Claude 的完整指标（训练结束后）：
  1. DirAcc（目标 >60%，参考 P0 曲线，epoch 35 前可接受 <60%）
  2. Region F1（目标 >40%，全量+80 epochs 后的阶段目标）
  3. Region Sep（目标 >1.5，观察是否继续上升）
  4. Pearson（目标 >0.99，全量时应恢复）
  5. Overlap@K（目标 >45%，全量时应恢复）
特别注意：
  如果 epoch 50 时 DirAcc 已经 >60% 且 Region F1 >35%，
  可以提前汇报告知 Claude 观察是否需要继续训练到 80 epochs