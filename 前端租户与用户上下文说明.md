# 前端租户与用户上下文说明

> 适用阶段：当前本地开发阶段
>
> 当前结论：可以理解并预留，但暂时不必重投入实现

---

## 1. 这是什么

“前端租户与用户上下文”指的是：当前端向 V2 后端发起一次分析请求时，除了传 `query`、`viewport`、`session_id` 之外，还额外传递：

1. 当前请求属于哪个租户/组织：`tenant_id`
2. 当前请求是哪个用户发起的：`user_id`
3. 当前用户的角色/权限边界：如 `role`、`permissions`

常见传递方式是 HTTP Header：

```http
X-Tenant-Id: acme-cn
X-User-Id: analyst-li
```

也可以通过 token 解析，但本质上都是让后端知道“谁在用系统、属于谁、能做什么”。

---

## 2. 为什么企业产品需要它

在企业环境里，V2 不只是一个单机工具，而是一个要被多人、多组织长期使用的 Agent 系统。

没有租户/用户上下文，后端就只能看到：

1. 有人发来一条 query
2. 有一块 viewport
3. 有一个 session_id

它看不到：

1. 这是谁发的
2. 属于哪个公司/部门
3. 这个人是否有权限导出
4. 配额应该记到哪个组织
5. 审计日志应该挂到哪个账号

所以一旦系统进入企业场景，租户/用户上下文是配额、审计、权限、成本归属的基础。

---

## 3. 对当前本地阶段意味着什么

你当前明确说了：**目前只跑本地，没有上线需求。**

在这个阶段，完整的租户/用户体系不是第一优先级。

当前最合理的策略是：

1. 先把概念和接口理解清楚
2. 后端治理逻辑支持接收这类字段
3. 前端可以先不做真实登录系统
4. 保持默认本地身份即可

也就是说：

**现在需要的是“保留能力入口”，而不是“立即做完整身份系统”。**

---

## 4. 当前本地阶段的推荐做法

### 方案 A：完全暂缓，只保留后端兼容能力

做法：

1. 前端继续以匿名本地模式运行
2. 后端如果没收到 `tenant_id/user_id`，则自动使用默认值
3. 默认值可以是：
   - `tenant_id = local-dev`
   - `user_id = local-user`

优点：

1. 对当前本地开发最轻量
2. 不会打断主开发节奏
3. 仍为未来企业化预留协议入口

缺点：

1. 前端治理面板里看到的身份信息是固定值
2. 不适合多人本地并发共用环境

### 方案 B：本地假身份上下文，但不做真实登录

做法：

1. 前端本地保存一个简单对象：

```json
{
  "tenantId": "local-dev",
  "userId": "richard",
  "role": "owner"
}
```

2. 每次调用 V2 API 自动带上：
   - `X-Tenant-Id`
   - `X-User-Id`

3. 前端控制台显示当前本地身份

优点：

1. 更接近未来企业链路
2. 容易验证 quota/audit 归属是否正确
3. 不需要真正接入认证系统

缺点：

1. 仍然只是本地模拟身份，不是真实登录
2. 会增加一点当前前端状态管理复杂度

---

## 5. 当前阶段建议

结合当前项目阶段，我建议采用：

**方案 A 到方案 B 之间的中间形态：**

1. 后端继续兼容并接受 `tenant_id/user_id`
2. 当前前端暂不做完整身份系统
3. 若后续你要验证治理链路，再加一个本地身份配置层即可

换句话说：

**现在不必把“租户/用户上下文打通”当成主任务。**

更高优先级仍然是：

1. V2 专属前端链路
2. 可靠性控制面
3. 审计、评测、报告链路
4. 企业级 GIS 工作流能力

---

## 6. 它将来会落在哪些代码点

如果后面进入企业接入阶段，这个能力主要会落在以下位置：

### 前端

1. [aiService.js](D:/AAA_Edu/TagCloud/vite-project/src/utils/aiService.js)
   作用：统一给 V2 请求带 `X-Tenant-Id / X-User-Id`

2. [V2AgentTraceMode.vue](D:/AAA_Edu/TagCloud/vite-project/src/views/V2AgentTraceMode.vue)
   作用：展示当前上下文和治理结果

3. `sessionStorage` 或本地用户配置模块
   作用：保存当前本地身份

### 后端

1. [analysis.js](D:/AAA_Edu/TagCloud/vite-project/V2-Agent-backend/src/routes/analysis.js)
   作用：读取 header 并进入 audit/quota

2. [quota-governor.js](D:/AAA_Edu/TagCloud/vite-project/V2-Agent-backend/src/runtime/quota-governor.js)
   作用：按租户/用户记账

3. [audit-event-store.js](D:/AAA_Edu/TagCloud/vite-project/V2-Agent-backend/src/observability/audit-event-store.js)
   作用：按租户/用户写审计事件

---

## 7. 什么时候该正式做它

满足以下任一条件时，就应该把它正式纳入开发主线：

1. 多个真实用户开始共用同一套 V2 环境
2. 需要区分不同组织/部门的使用数据
3. 需要做用户级配额或权限控制
4. 需要让审计记录具备“谁发起的”真实归属
5. 准备上线到测试环境或生产环境

---

## 8. 结论

前端租户与用户上下文，本质上是企业治理基础设施的一部分。

但对你当前“只跑本地、没上线需求”的阶段来说：

1. 它不是必须马上完成的主任务
2. 应该先理解并保留接口能力
3. 真正重投入实现可以延后到测试环境/上线前

所以当前最合理的结论是：

**先不把它当成当前主战场，只保留可接入能力；等进入多人/上线阶段，再做成完整企业身份链路。**
